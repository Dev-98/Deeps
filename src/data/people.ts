/**
 * Who this is for.
 *
 * `her.name` is the word that fills the screen in chapter 09 — the single
 * most important string in the project. The pet names are what the writing
 * uses everywhere else, so the letters sound like you and not like a
 * greetings card.
 */
export const her = {
  /** The big one. Gold letters, two metres tall. */
  name: "Ishuu",
  /** Used in the writing. Swap the order if one of them fits better. */
  pet: {
    soft: "Buubuu",
    casual: "babes",
    grand: "my queen",
    little: "bacha",
  },
};

export const me = {
  /** What she calls you — the same answer that opens the door. */
  name: "Guuguu",
};
